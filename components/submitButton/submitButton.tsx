import React from "react";
import styles from './submitButton.module.css'
const SubmitButton = () => {
  return <button type="submit" className={styles.button}>Submit</button>;
};

export default SubmitButton;
